/**
 * Interface que representa as credenciais de login
 * 
 * Campos:
 * - username: Email do usuário (usado para autenticação)
 * - password: Senha do usuário
 */
export interface Credenciais {
  username: string;
  password: string;
}